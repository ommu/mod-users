data_submenu:
  invite:
    label: Invites
    url: ['invite/admin']
    select: controller
  subscribe:
    label: Subscribes
    url: ['newsletter/admin']
    select: controller
  forgot:
    label: Forgot(s) Password
    url: ['o/forgot']
    select: controller
  verify:
    label: Verify(s) Email
    url: ['o/verify']
    select: controller
userlevel_submenu:
  view:
    label: Detail
    url:
      0: 'setting/level/view'
      id: $_GET*id
    select: action
  update:
    label: Update
    url:
      0: 'setting/level/update'
      id: $_GET*id
    select: action
  user:
    label: User
    url:
      0: 'setting/level/user'
      id: $_GET*id
    select: action
  message:
    label: Message
    url:
      0: 'setting/level/message'
      id: $_GET*id
    select: action
  delete:
    label: Delete
    url:
      0: 'setting/level/delete'
      id: $_GET*id
    htmlOptions:
      data-confirm: Are you sure you want to delete this item?
      data-method: post
    select: action